import torch

def load_model(model_path="model/lip_reader_model.pth"):
    # Dummy function for now
    print("Model loading is mocked (no actual model yet)")
    return None

def predict_lip_sequence(sequence, model=None):
    # Mocked prediction function
    return "Hello (Mock Prediction)"